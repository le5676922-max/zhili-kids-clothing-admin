<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.java.mapper.ProductFavoriteMapper">

    <insert id="insert">
        INSERT IGNORE INTO product_favorites (user_id, product_id, created_at)
        VALUES (#{userId}, #{productId}, NOW())
    </insert>

    <delete id="delete">
        DELETE FROM product_favorites WHERE user_id = #{userId} AND product_id = #{productId}
    </delete>

    <select id="exists" resultType="boolean">
        SELECT COUNT(*) > 0 FROM product_favorites WHERE user_id = #{userId} AND product_id = #{productId}
    </select>

    <select id="selectByUserId" resultType="java.lang.String">
        SELECT product_id FROM product_favorites WHERE user_id = #{userId} ORDER BY created_at DESC
    </select>

    <delete id="deleteByUserId">
        DELETE FROM product_favorites WHERE user_id = #{userId}
    </delete>
</mapper>
